package com.bcv.hw.FinalProject;

public class Formulas {
    public static void main(String[] args) {
        double k, kpol, S, b, L, F, G, y, M, R, c, e, g, e1, g1, angle;


        //for C23
/*        S = 50.0; //thick
        b = 2.0; //gap - zazor
        L = 1500; //length


        F = S * b + 1.5 * e * g;
        F = S * b + 1.57 * R * R + (S - c - R) * 2 * R + (S - c - R) * (S - c - R) * Math.tan(angle) + 0.75 * (e * g + e1 * g1);
        G = F * L * y;
        M = G * k * kpol;*/
    }
}
