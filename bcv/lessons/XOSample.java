package com.bcv.lessons;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

public class XOSample {

   /*static String winArray[] = {"012", "345", "678", "036", "147", "258", "048", "246"};

    public static void main(String[] args) {
        System.out.println(winArray.length);
    }

    static void chekWinner(String user) {
        char[] userC = user.toCharArray();
        List<String> list = Arrays.asList(winArray);
        for (char win : userC) {
            String s = "" + win;
            List winList = checkWinnerPartTwo(s, list);
            v

            }
        }

    }

    void checkWinnerPartTwo(String c, List<String> list) {
        List<String> winUser new ArrayList<>();
        for (String s : list) {
            if (s.contains(c)) {
                winUser.add(s);
            }
        }

    }*/
}
